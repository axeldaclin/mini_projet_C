//jeu aléatoire
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() 
{
    srand(time(NULL));
    int n0=(rand() % 100) + 1;
    int n=0;
    int i=1;
    printf("saisir le nombre à comparer: \n");
    scanf("%d",&n);
    while (n!=n0)
    {
       if (n<n0)
        {
        printf("trop petit ! \n");
        }
        else if (n>n0)
        {
           printf("trop grand ! \n");
        }
    printf("saisir un autre nombre: \n");
    scanf("%d",&n);
    i=i+1;
    }    
    printf("Bravo, tu as réussi en %d essais \n", i);
    return 0;
}
